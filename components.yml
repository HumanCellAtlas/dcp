---
#
# HCA DCP Component Manifest
#
# Please ensure you are on the correct deployment branch before changing this file.
# Do not change the version on the master branch.
#
deployed_component_versions:
  # component-identifier: component-version
  # e.g.
  # ingest: v0.1.2
  upload: v2.0.1
  metadata-schema: 4.4.0
  ingest:
    ingest-accessioner: v0.4.0
    ingest-broker: v0.7.0
    ingest-core: v0.6.0
    ingest-exporter: v0.6.2
    ingest-staging-manager: v0.5.0
    ingest-state-tracking: v0.6.0
    ingest-validator: v0.5.1
  data-store: prod-2018-07-17-15-37-03.release
