---
#
# HCA DCP Component Manifest
#
# Please ensure you are on the correct deployment branch before changing this file.
# Do not change the version on the master branch.
#
deployed_component_versions:
  # component-identifier: component-version
  # e.g.
  # ingest: v0.1.2
  upload: staging-20180206T041342Z
  metadata-schema: 4.4.0
  ingest:
    ingest-broker: 0.1.3
    ingest-exporter: 0.1.5
    ingest-validator: 0.1.4
    ingest-core: 0.1.2
    ingest-accesioner: 0.1.1
    ingest-archiver: 0.1.1
    ingest-staging-manager: 0.1.1
  greenbox:
    greenbox-cromwell-tools: 0.1.0
    greenbox-cromwell: 30-047d48f
    greenbox-lira: v0.4.3
    greenbox-pipeline-tools: v0.1.7
    greenbox-skylab: 0.2.1
