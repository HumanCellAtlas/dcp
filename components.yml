---
#
# HCA DCP Component Manifest
#
# Please ensure you are on the correct deployment branch before changing this file.
# Do not change the version on the master branch.
#
deployed_component_versions:
  # component-identifier: component-version
  # e.g.
  # ingest: v0.1.2
  upload: v2.3.5
  metadata-schema: http://api.ingest.data.humancellatlas.org/schemas/search/latestSchemas
  azul: 0ff6c960735a0cc9772a657be1fbfcda330dc36f
  secondary-analysis:
    falcon: v0.1.3
    lira: v0.14.0
    pipeline-tools: v0.38.0
  ingest:
    ingest-accessioner: v0.5.0
    ingest-broker: v0.8.0
    ingest-core: v0.7.2
    ingest-exporter: v0.7.1
    ingest-staging-manager: v0.5.1
    ingest-state-tracking: v0.7.0
    ingest-validator: v0.6.0
    ingest-ui: v0.5.2
  data-store: production-2018-09-18-15-53-48.release bb86b74
